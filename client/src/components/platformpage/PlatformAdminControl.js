

/**
 * 
 * @param {props} props takes callback
 * @returns 
 */
export default function PlatformAdminControl(props) {
    const callback = props.callback

    return (
        <div>
            <a className="waves-effect waves-light btn modal-trigger red">Add Admin</a>
        </div>
    )
}